#!/usr/bin/env ruby
# -*- coding: utf-8 -*-

require 'rubygems'
arr = Gem.latest_load_paths.select { |x|
  x.match( /\/sekka[-]/ )
}

if 0 == arr.size 
  STDERR.puts "Error: gem of sekka is not installed yet..."
  exit( 1 )
else
  puts arr[0].sub( /\/lib$/, "" )
end
